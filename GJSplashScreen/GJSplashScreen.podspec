

Pod::Spec.new do |s|

  s.name         = "GJSplashScreen"
  s.version      = "0.0.1"
  s.summary      = "这是一个现实闪屏图的库"
  s.license      = "MIT"
  s.platform     = :ios, "7.0"
  s.source_files = "GJSplashScreen/code/*.{h,m}"
  s.resources    = "GJSplashScreen/resource/*.{png,jpg,xib}" 

end
